name: Demostración de un pipelinede GithubActions

on:
  push:
    branches:
      - main
      - develop

jobs:
  saludo:
    runs-on: ubuntu-latest

    steps:
      - name: Saludo desde main
       run: echo"Hola desde main, mi nombre es Ian"

       - name: Saludo desde develop
       run: echo"Hola desde develop, mi nombre es Ian"
